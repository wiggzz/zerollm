don't keep unused code around

When you find bugs, correctness issues, operational footguns, or non-blocking
cleanup work while investigating or implementing, add them to TODO.md instead of
leaving them only in chat history.
